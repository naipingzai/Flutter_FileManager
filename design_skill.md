# 导入式文件管理器（Import-based File Manager）系统布局与功能设计文档

> **文档性质**：这是**基于参考工程 [AdvanceFileManager](https://github.com/naipingzai/AdvanceFileManager) 布局与交互风格**的**定制设计方案**。参考工程是一个"直接访问系统文件"的文件管理器；本设计在其基础上改造为**导入式文件管理器**，并新增**标签系统**。
>
> **核心差异点（相对参考工程）**：
> 1. 文件**不能直接访问**系统存储，必须先"导入"到应用管理的私有空间才能被浏览管理。
> 2. 顶部工具栏比参考工程**多两个按钮**（「导入」按钮 + 「导出」按钮）。
> 3. 新增**标签系统**：所有导入的文件**默认自动打上标签**；支持**单选、多选打标签**；提供**标签搜索**。
>
> **适用平台**：Android 13.0+（设计规范针对 Android 15 / API 35+）
> **技术栈**：Kotlin + Material Design 3

---

## 目录

1. [应用定位与信息架构](#1-应用定位与信息架构)
2. [导入式模型 vs 直接访问模型（关键区别）](#2-导入式模型-vs-直接访问模型关键区别)
3. [设计系统规范（设计令牌）](#3-设计系统规范设计令牌)
4. [页面布局模式分类](#4-页面布局模式分类)
5. [核心页面：文件列表主界面](#5-核心页面文件列表主界面)
6. [交互机制详解：单击 / 长按 / 单选 / 多选](#6-交互机制详解单击--长按--单选--多选)
7. [菜单弹出详解（每个菜单的弹出位置与内容）](#7-菜单弹出详解每个菜单的弹出位置与内容)
8. [对话框弹出详解（每个对话框的弹出位置与内容）](#8-对话框弹出详解每个对话框的弹出位置与内容)
9. [导入 / 导出功能设计](#9-导入--导出功能设计)
10. [标签系统设计（核心新增）](#10-标签系统设计核心新增)
11. [标签搜索功能设计](#11-标签搜索功能设计)
11b. [内置文件查看器详解](#11b-内置文件查看器详解模式-c--模式-a)
12. [侧边抽屉导航](#12-侧边抽屉导航)
13. [设置页 / 显示设置页 / 标签设置页](#13-设置页--显示设置页--标签设置页)
13b. [辅助页面详解（回收站 / 关于 / 存储）](#13b-辅助页面详解回收站--关于--存储)
14. [文件属性与文件详情](#14-文件属性与文件详情)
15. [数据模型与状态管理](#15-数据模型与状态管理)
16. [复刻实现要点清单](#16-复刻实现要点清单)

---

## 1. 应用定位与信息架构

一款 Material Design 3 风格的**导入式文件管理器**：用户通过「导入」把外部文件收纳进应用私有空间统一管理；通过「导出」将文件分享回系统或其他位置。所有文件都被**标签系统**标记和检索。

### 1.1 顶层导航结构

```
启动 → LibraryActivity (文件库主界面)
        │
        ├─ 侧边抽屉 NavigationFragment
        │     ├─ 库：全部文件 / 收藏 / 最近导入
        │     ├─ 标签区：全部标签 / 各标签快捷入口
        │     ├─ 回收站
        │     └─ 菜单：设置 / 显示设置 / 标签管理 / 关于
        │
        ├─ 主界面操作 → 内置查看器（图片/视频/音频/PDF/文本/Hex/电子书/CSV）
        ├─ 单选/多选 → 打标签 / 导出 / 删除 / 属性 / 分享
        ├─ 顶部工具栏 → [视图排序] [导入] [导出]  ← 三个常驻按钮（比参考工程多"导入、导出"）
        └─ 标签搜索 → 按标签过滤文件列表
```

### 1.2 页面清单总览

| 页面 | 类型 | 宿主 | 布局模式 |
|------|------|------|----------|
| 文件库（核心） | 主界面 | 文件库页 | 抽屉+持久底部栏+顶部栏 |
| 侧边导航 | 抽屉面板 | 内嵌于文件库 | 可滚动列表 |
| 设置 | 子页面 | 设置页 | 顶部栏+内容区 |
| 显示设置 | 子页面 | 显示设置页 | 顶部栏+内容区 |
| 标签管理 | 子页面 | 标签管理页 | 顶部栏+内容区 |
| 文件工具 | 子页面 | 工具宿主页 | 双按钮+列表 |
| 标签搜索 | 子页面 | 文件库内嵌 | 搜索框+标签筛选片 |
| 查看器 | 子页面 | 各自查看页 | 整屏叠层 |
| 文件属性 | 对话框 | 对话框宿主 | 多页签对话框 |
| 打标签 | 对话框 | 对话框宿主 | 标签多选对话框 |

---

## 2. 导入式模型 vs 直接访问模型（关键区别）

这是本设计不同于参考工程的根本所在。

| 维度 | 参考工程（直接访问） | 本设计（导入式） |
|------|---------------------|------------------|
| 文件来源 | 直接读系统存储/SAF/Root | 通过「导入」按钮从系统选入应用私有空间 |
| 数据存放 | 原路径 | 应用私有目录（如 `files/library/`），应用复制一份 |
| 文件列表数据 | 实时扫描目录 | 读应用库中的文件记录 |
| 浏览限制 | 可自由导航任意路径 | 只能浏览已导入的库内容（无路径自由导航）|
| 文件操作 | 复制/移动/删除原文件 | 管理应用内的副本，删除即从库移除 |
| 标签 | 无 | **核心功能**，每个文件有标签集合 |
| 退出方式 | 面包屑自由回退 | 返回库根；无路径越权 |

**面包屑导航改造**：导入式模型无任意路径导航，面包屑简化为"库根 / 分类视图 / 标签筛选"层级，或直接移除。

---

## 3. 设计系统规范（设计令牌）

与参考工程完全一致，保证视觉统一。**禁止硬编码**。

### 3.1 间距层级（spacing 令牌）

| 层级 | 值 | 使用场景 |
|------|-----|----------|
| `spacing_xxs` | 2dp | 标题/副标题间隙 |
| `spacing_xs` | 4dp | 紧密元素间、按钮间 |
| `spacing_sm` | 8dp | 按钮间距、卡片外边距 |
| `spacing_md` | 12dp | 中等分组间距 |
| `spacing_lg` | 16dp | 卡片内 padding、标准间距 |
| `spacing_xl` | 24dp | 分组间距 |
| `spacing_xxl` | 32dp | 大留白 |

页面级边距 `screen_edge_margin = 16dp`；对话框水平 padding 用 `?dialogPreferredPadding`。

### 3.2 字体大小（textAppearance 令牌）

统一 `?textAppearance*` 短格式引用。

| 用途 | 令牌 | 约值 |
|------|------|------|
| 大标题 | `?textAppearanceTitleLarge` | 22sp |
| 中标题（列表项/卡片标题） | `?textAppearanceTitleMedium` | 16sp |
| 小标题 | `?textAppearanceTitleSmall` | 14sp |
| 正文大 | `?textAppearanceBodyLarge` | 16sp |
| 正文中 | `?textAppearanceBodyMedium` | 14sp |
| 正文小（描述/副标题） | `?textAppearanceBodySmall` | 12sp |
| 列表项主标题 | `?textAppearanceListItem` | 16sp |
| 列表项副标题 | `?textAppearanceListItemSecondary` | 14sp |
| 标签中 | `?textAppearanceLabelMedium` | 12sp |
| 标签小 | `?textAppearanceLabelSmall` | 11sp |

### 3.3 卡片样式

圆角统一 12dp。

| 样式 | elevation | stroke | 场景 |
|------|-----------|--------|------|
| `Widget.App.CardView.Filled` | 2dp | 0dp | 设置项、功能卡、标签卡 |
| `Widget.App.CardView.Outlined` | 0dp | 1dp | 强调卡 |

### 3.4 列表项与图标尺寸

| 类型 | 尺寸 | 场景 |
|------|------|------|
| 单行列表项 | 56dp (`?listPreferredItemHeightSmall`) | 导航项、设置项 |
| 双行列表项 | `two_line_list_item_height` (48dp) | 文件行 |
| 图标 | `icon_size` (24dp) | 列表项左侧图标 |
| 大图标 | `large_icon_size` (40dp) | 缩略图、关于页 |
| 触控目标 | `touch_target_size` (48dp) | 可点击区 |
| 徽章 | `badge_size` (18dp) | 标签徽章 |

### 3.5 组件规范
- 开关统一使用 Material 风格开关（Switch），禁用旧式复选框。
- 单选选择用列表式单选对话框，选项呈竖排可点选。
- 主操作按钮用实心强调色按钮，次操作按钮用浅色（tonal）按钮。

### 3.6 颜色与主题
- 启用 **Material You 动态取色**，支持纯黑夜间模式。
- 顶部栏背景用"栏面"色，抽屉背景用"界面"色，标签可用彩色 chip 区分。
- 次要文字用较浅的界面色变体，强调文字/标题用主题强调色。

### 3.7 用户可调缩放
显示设置页可缩放 7 类参数（50%~200%）：字体、间距、列表项高度、图标、页面边距、对话框内边距、按钮间距。这些参数改变时全局实时生效，无需重启。预设：紧凑/默认/宽松/自定义。

### 3.8 背景模糊遮罩
弹出菜单/对话框显示时，主界面会叠加一层可调强度的暗色遮罩（默认 50%），使背景变暗突出浮层；浮层关闭后遮罩自动消失。

---

## 4. 页面布局模式分类

所有页面可归纳为 6 种布局模式。以下使用通用 UI 描述（组件的正式名称仅为便于指代布局结构）。

### 模式 A：顶部栏 + 可滚动内容区（设置类）
```
整页根容器 (自动适配状态栏)
  ├─ 顶部栏 (背景=栏面色, 滚动时浮起)
  │    └─ 工具栏 (标准高度)
  └─ 滚动内容区 (充满剩余空间)
       └─ 列表 / 滚动容器 / 内容区
```
用于：设置、显示设置、标签管理、关于、回收站、存储列表。

### 模式 B：双按钮 + 进度条 + 列表（工具页）
```
竖排布局
  ├─ 一排两个按钮 (横向, 内边距8dp)
  │    ├─ 主操作按钮 [占一半, 右留4dp]
  │    └─ 次操作按钮 [占一半, 左留4dp]
  ├─ 水平进度条 (默认隐藏)
  ├─ 居中空状态文字 (默认隐藏)
  └─ 列表 (边缘不裁剪, 底部留8dp)
```

### 模式 C：整屏叠层（媒体查看器）
```
整屏根容器
  ├─ 内容层 (多图滑动区/播放画面/网页渲染区/图片/列表)
  ├─ 居中圆形进度条 (默认隐藏)
  ├─ 居中错误/空状态文字 (默认隐藏)
  └─ 顶部浮层工具栏 (背景=栏面色)
```

### 模式 D：对话框表单
```
对话框内容容器 (顶部留出标题分隔间距)
  └─ 可滚动内容 (上下出现滚动指示条)
       └─ 竖排表单区 (左右按对话框标准边距, 上下留8dp)
```

### 模式 E：单列滚动 + 卡片（详情页）
整页单列可滚动，内容由若干卡片纵向堆叠组成。

### 模式 F：标签搜索（搜索框 + 筛选片）
```
顶部栏 + 内容区
  └─ 内容区 (竖排)
       ├─ 搜索框 (提示"搜索文件名或标签")
       ├─ 一排横向滚动的标签筛选片 (可多选)
       └─ 过滤结果列表
```

---

## 5. 核心页面：文件列表主界面

### 5.1 整体布局结构

```
整页根容器
  └─ 侧滑抽屉框架 (自动适配状态栏)
       ├─ 主内容区 (可全屏拖动)
       │    └─ 持久栏容器 (允许底部栏常驻屏幕底部, 自动适配状态栏)
       │         ├─ [顶部栏区]（5.2）
       │         │    └─ [内容区]（5.3）
       │         ├─ [底部栏]（5.4）
       │         └─ 背景遮罩层 (浮层弹出时显示, 默认隐藏)
       └─ 侧边导航面板 (靠左, 宽度自适应内容, 背景=界面色)
```

### 5.2 顶部栏区（**含新增导入/导出按钮**）

```
顶部栏 (背景=栏面色, 滚动时浮起, 随列表滚动收起/展开)
  ├─ 高度=工具栏高度的一层
  │    ├─ 常规工具栏  ← 日常显示
  │    │     · 导航图标"三横线"打开抽屉
  │    │     · 标题下方副标题：文件总数/当前筛选
  │    │     · 右侧常驻按钮（见下表）
  │    └─ 多选工具栏 (默认隐藏)  ← 多选时淡入覆盖
  └─ 面包屑导航栏  ← 显示"库/分类/标签"层级，可点击跳转
```

**工具栏右侧按钮顺序（常驻，由左到右）：**

| 位置 | 按钮 | 图标 | 行为 | 相对参考工程 |
|------|------|------|------|-------------|
| 右1 | 视图/排序 | sort 图标 | 弹出排序/视图子菜单 | 同参考 |
| **右2** | **导入** | **download/import 图标** | 打开系统文件选择器导入文件 | **新增** |
| **右3** | **导出** | **upload/export 图标** | 导出当前选中的文件 | **新增** |

> 溢出菜单仍保留：新建文件夹、打标签、显示隐藏文件、设置入口等。

**导入按钮行为**：点击 → 弹出系统文件选择器（可多选）→ 选择后复制到应用私有库 → 每个文件**自动打上"默认标签"** → 列表刷新 + 提示 "已导入 N 个文件"。

**导出按钮行为**：点击 → 若未选中任何文件则先进入多选（或提示）；若已选中 → 弹出系统保存界面 → 从应用库复制到目标位置。

### 5.3 内容区

```
内容区 (充满剩余空间)
  ├─ 居中圆形进度条 (加载中显示)
  ├─ 居中错误提示 (加载失败显示)
  ├─ 居中空状态 ("库为空，点击右上角导入" + 图标)
  └─ 下拉刷新容器
       └─ 文件列表 (底部留出 88dp 空间给浮动按钮, 边缘不裁剪)
```

### 5.4 底部栏（持久栏容器底部常驻）

```
底部栏 (背景=界面色, 有轻微上浮阴影, 自动适配底部状态栏)
  └─ 工具栏
       ├─ 导航图标"关闭×"
       └─ 文件名输入框 (创建文件时显示, 默认隐藏)
```
底部栏用于：粘贴状态（导入式下较少用）、多选操作栏兜底、或创建文件输入。

### 5.5 文件列表项

#### 列表模式（双行横向）

```
整行 (高约48dp, 可选中背景+点击波纹, 垂直居中)
  ├─ 左侧图标区 (48dp 正方形触控区)  ← 多层级叠加图标
  │    ├─ 文件类型图标 (24dp)
  │    ├─ 缩略图 (40dp)
  │    ├─ 应用角标 (19dp, 右下角)
  │    ├─ 状态徽章 (18dp, 右下角, 圆形背景)  ← 可显示标签色/默认标签标识
  │    └─ 多选对勾徽章 (18dp, 右下角)
  ├─ 中间文字区 (占满剩余宽度, 垂直排列)
  │    ├─ 文件名 (单行, 过长滚动省略)
  │    ├─ 描述 (单行, 时间·大小)
  │    └─ 标签行 (标签片, 显示文件标签, 无标签自动隐藏)  ← 新增
  └─ 右侧菜单按钮 (48dp, "三个竖点")  ← 弹出单文件操作菜单
```

#### 网格模式（卡片缩略图）

```
整卡 (垂直排列, 可选中背景)
  ├─ 16:9 缩略图区 (等宽铺满)
  │    ├─ 文件夹缩略图底 (文件夹专用)
  │    ├─ 居中图标 (40dp)
  │    ├─ 圆角缩略图
  │    └─ 描边 (非文件夹)
  ├─ 底部一行 (高56dp, 垂直居中)
  │    ├─ 左侧图标区 (48dp 触控区) + 类型图标/徽章
  │    ├─ 文件名 (占满剩余, 居中, 最多2行)
  │    └─ 右侧菜单按钮 (溢出菜单)
  └─ 标签行 (标签片)  ← 新增
```

网格列数：按屏幕宽度每约 180dp 一列，最少 2 列。
### 5.6 顶部工具栏菜单

| 项 | 显示 | 行为 |
|----|------|------|
| 视图/排序（sort 图标） | 常驻右1 | 视图（列表/网格）+ 排序（名称/类型/大小/最近修改 + 目录优先）|
| **导入** | 常驻右2 | 打开系统选择器导入 |
| **导出** | 常驻右3 | 导出选中文件 |
| 打标签 | 溢出 | 对选中/当前文件打标签 |
| 新建文件夹 | 溢出 | 库内建分类文件夹 |
| 显示隐藏文件 | 溢出 | 复选框 |

---

## 6. 交互机制详解：单击 / 长按 / 单选 / 多选

> 这是核心交互，**必须精确复刻**。以下全部是用户可感知的界面行为，不含实现。

### 6.1 交互触点总表（针对每个文件行）

每个文件行有 4 个可交互区域：

| 触点 | 位置 | 手势 | 效果 |
|------|------|------|------|
| **整行** | 除图标区/菜单按钮外的区域 | 单击 | 见 6.2 分支 |
| **整行** | 同上 | 长按 | 见 6.3 分支 |
| **图标区** | 行最左侧 48dp 正方形区域 | 单击 | 恒为"切换该文件选中状态" |
| **菜单按钮** | 行最右侧"三个竖点"图标 | 单击 | 弹出该文件的单项操作菜单（第 7.1 节）|

**区域优先级**：菜单按钮和图标区各自独立响应，点击它们**不会**触发整行的单击/长按。

### 6.2 单击文件行（根据有无选中切换行为）

整行单击的最终效果取决于**当前是否已有选中文件**：

- **当前没有任何选中**（普通浏览态）：单击 = **打开**该文件。
  - 文件夹 → 进入下一层。
  - 文件 → 按类型进入对应查看器（图片/视频/音频/PDF/文本/Hex/CSV/电子书）或交给系统打开。
- **当前已有选中**（多选态）：单击 = **切换**该文件的选中状态（加入或移出多选），顶部多选工具栏的计数"已选择 N 项"实时更新。

### 6.3 长按文件行（进入多选 / 打开）

长按的最终效果同样取决于当前选中状态：

- **当前没有任何选中**：长按 = **选中该文件并进入多选模式**。此时顶部淡入多选工具栏，该行背景高亮、右下角出现对勾徽章。
- **当前已有选中**：长按 = **打开**该文件（与单击互换，便于在多选态直接预览）。

**进入多选模式的界面变化链（用户视角）：**
```
长按某文件
  → 该文件被选中（背景高亮 + 对勾徽章）
  → 顶部普通工具栏被"多选工具栏"淡入覆盖
  → 多选工具栏标题显示"已选择 1 项"
  → 多选工具栏右侧出现操作按钮（剪切/复制/删除/全选 + 溢出）
  → 继续单击其他行可追加/移除选中，计数实时更新
```

多选工具栏的按钮按选中文件属性动态调整：
- 选中只读文件 → 隐藏"剪切""删除"。
- 选中全为压缩包 → "复制"变为"解压"。
- "压缩"功能未启用 → 隐藏"压缩"。

### 6.4 选中状态的判定（选择器模式差异）

在普通浏览模式，任何文件都可选。
在作为"系统文件选择器"被调用的模式下，可选择性受限：
- 选文件模式 → 只能选文件（非目录），且 MIME 需匹配。
- 选目录模式 → 只能选目录。
- 单选模式 → 新选中一个会先自动清除之前的选中。

### 6.5 选中状态在界面上的表现（如何"看到"选中了）

选中态由三处视觉共同体现：

1. **整行背景高亮**：
   - 选中行背景变为**半透明白色圆角高亮块**；未选中则透明。
   - 选中/取消时有**短促的淡入淡出动画**。
   - 网格模式下高亮块带圆角和波纹前景，与缩略图卡片融为一体。

2. **右下角对勾徽章**：
   - 每个文件行的图标区右下角固定有一个小对勾圆形徽章。
   - 该行选中时对勾徽章自动显现，未选中时隐藏。

3. **图标与文字联动**：
   - 选中时行内图标与文字颜色按主题自动变化（更醒目）；禁用态自动变淡。

**刷新表现**：切换选中时，只更新受影响的那一行（选中视觉变化），整列表不重载、不重播动画，保证流畅。

### 6.6 全选

- 点击多选工具栏"全选"→ **当前列表可见的全部可选文件**被选中（不跨页、不选不可选文件）。
- 多选工具栏标题更新为"已选择 N 项"。

### 6.7 多选模式下的工具栏

**两种工具栏形态**：
- **常规工具栏**：非多选时显示，含标题、面包屑、导入/导出/视图排序按钮。
- **多选工具栏**：多选时以淡入动画覆盖在常规工具栏之上（同一位置）。

**多选工具栏元素（从左到右）：**
```
[×关闭]  已选择 N 项     [剪切][复制][删除][全选] … [溢出]
```
- 最左"×" → 取消多选（清空所有选中）。
- 中央标题 → 显示已选数量。
- 右侧按钮区 → 操作按钮（图标常驻）+ 溢出菜单（更多操作）。

**多选菜单内容：**

| 项 | 图标 | 常驻/溢出 | 行为 |
|----|------|-----------|------|
| 剪切 | 剪刀 | 图标常驻 | 标记剪切（只读隐藏）|
| 复制 | 复制 | 图标常驻 | 标记复制（全压缩包时变"解压"）|
| 删除 | 垃圾桶 | 图标常驻 | 弹确认删除对话框 |
| 全选 | 全选 | 图标常驻 | 选中全部可见文件 |
| **打标签** | **标签图标** | 溢出 | **弹打标签对话框（作用全部选中）** |
| 导出 | 导出图标 | 溢出 | 导出全部选中 |
| 分享 | 分享 | 溢出 | 分享全部选中 |
| 属性 | info | 溢出 | 多文件属性 |

### 6.8 取消多选

- 点多选工具栏最左"×"，或按系统返回键 → 清空所有选中 → 多选工具栏淡出，恢复常规工具栏。

### 6.9 单选模式下打标签（不进入多选）

用户也可**不进入多选**，直接对**单个文件**打标签：
- 途径1：单击文件行菜单按钮 → 弹出菜单 → 选「打标签」→ 标签对话框（作用该文件）。
- 途径2：长按单个文件进入多选（只选中这一个）→ 点多选工具栏「打标签」。

### 6.10 多选工具栏的状态变化规律

- **开始多选**：首个文件被选中时，多选工具栏淡入，标题计数为 1。
- **追加/移除**：后续每次增减选中，标题计数实时变化，受影响行局部更新。
- **清空**：选中归零时多选工具栏淡出，恢复常规工具栏。
- **多选期间按返回键**：等同取消多选。

### 6.11 交互优先级总结

1. **菜单按钮/图标区优先于整行**：点它们只触发各自功能，不触发行单击/长按。
2. **图标区**：恒为"切换选中"，与整行语义独立。
3. **有选中时**：整行单击 = 切换选中，整行长按 = 打开。
4. **无选中时**：整行单击 = 打开，整行长按 = 进入多选。
5. **多选态**：顶部被多选工具栏覆盖，所有按钮作用于全部选中文件。

---

## 7. 菜单弹出详解（每个菜单的弹出位置与内容）

> **通用弹出机制**：每个菜单都是"弹出式菜单"（popup menu），**以触发它的按钮为锚点**，系统自动将其展开在按钮附近的合适位置（优先锚点下方，空间不足则上方，紧贴锚点对齐）。菜单按分组显示，组与组之间用**分隔线**隔开。
>
> **通用弹出前动作**：每次弹出前，主界面叠加一层暗色背景遮罩；菜单关闭后遮罩自动消失。

### 7.1 单文件操作菜单 —— 锚定在行尾"三个竖点"按钮

**触发**：单击文件行最右侧的"三个竖点"按钮。
**弹出位置**：以该按钮为锚点展开。因按钮在行尾靠右，菜单通常**在按钮左侧或左下方**展开（避免超出屏幕右缘）。每行一份独立菜单。
**内容动态**：弹出前根据**该文件**的属性决定哪些菜单项显示。

**菜单内容（按分组，组间有分隔线）：**

| 分组 | 菜单项 | 何时可见 |
|------|--------|----------|
| **常用** | 打开方式 | 已启用"打开方式"功能 |
| | 剪切 | 非选择器模式 且 非只读 |
| | 复制 | 非选择器模式（压缩包显示为"解压"）|
| | 删除 | 非只读 |
| | 重命名 | 非只读 |
| | 解压 | 该文件是压缩包 |
| | 压缩 | 非压缩包路径 且 已启用"压缩" |
| | 分享 | 已启用"分享" |
| **动态功能** | 按文件类型追加的相关工具 | 功能已启用 且 类型匹配 |
| **扩展** | 复制路径 | 已启用"复制路径" |
| | 添加书签 | 是目录 且 已启用 |
| | 属性 | 已启用"属性" |
| | **打标签（新增）** | 恒显示 |

**点击后**：各项执行对应操作（打开方式/剪切/复制/删除/重命名/解压/压缩/分享/复制路径/添加书签/属性/**打标签**），动态功能项进入对应工具。

### 7.2 多选工具栏菜单 —— 锚定在顶部多选工具栏

**触发**：进入多选模式后，点击顶部多选工具栏右侧"三个竖点"。
**弹出位置**：以多选工具栏为锚点，在**屏幕顶部工具栏右下方**展开。
**内容**：打标签、导出、删除、全选、剪切、复制、分享等（图标常驻 + 溢出文字混合，见 6.7 表）。

### 7.3 顶部视图/排序子菜单 —— 锚定在"视图/排序"图标

**触发**：点击工具栏右1「视图/排序」图标（sort 图标，常驻显示）。
**弹出位置**：以该图标为锚点，在工具栏图标下方展开。
**内容结构（两个分组，组间分隔线）：**
```
视图组（单选）：
    列表   网格
排序组（单选）：
    名称   类型   大小   最近修改
    目录优先（可勾选）
```
当前生效的视图/排序项会**打勾标记**。

### 7.4 顶部溢出菜单 —— 锚定在工具栏最右"三个竖点"

**触发**：点击工具栏最右侧溢出"三个竖点"。
**弹出位置**：以溢出按钮为锚点，在工具栏右下方展开。
**内容**：打标签、新建文件夹、显示隐藏文件（复选）、设置入口等。

### 7.5 全部菜单弹出位置速查表

| 菜单 | 锚定 View | 展开位置 | 触发 |
|------|-----------|----------|------|
| 单文件操作菜单 | 行尾 menuButton | 按钮左/下方 | 单击三个竖点 |
| 多选工具栏菜单 | overlayToolbar | 顶部工具栏下方 | 多选时点三个竖点 |
| 视图/排序子菜单 | sort 图标 | 图标下方 | 单击 sort 图标 |
| 顶部溢出菜单 | 溢出按钮 | 工具栏右下方 | 单击三个竖点 |

---

## 8. 对话框弹出详解（每个对话框的弹出位置与内容）

> **通用弹出机制**：所有对话框都是**屏幕中央弹出**的模态对话框，内容为表单/列表的自定义区域，底部有操作按钮。对话框弹出时，主界面背景叠加暗色遮罩（强度随用户设置），使背景变暗突出对话框。

### 8.1 打标签对话框（核心新增，单选/多选共用）

**弹出位置**：屏幕中央。
**触发场景与作用对象**：
- 多选工具栏「打标签」→ 作用对象 = 全部选中文件。
- 单文件菜单「打标签」→ 作用对象 = 该单个文件。
- 详情/属性页「编辑标签」→ 作用对象 = 该文件。

**内容结构：**
```
标题："为 N 个文件打标签"  (N 为作用文件数)
[可滚动内容区]
  ├─ [已有标签区] 一排可多选的标签片(勾选片)
  │     每个标签片默认高亮"作用文件中已包含"的标签
  ├─ [新建标签区] 文本输入框（提示"输入新标签名"）
  │     + 回车确认 / "添加"按钮
  └─ [常用标签] 建议标签片（最近使用，点击即加入）
底部按钮：取消 / 确定
```
**确定逻辑**：把"勾选标签 + 新建标签"合并为作用文件的新标签集合并保存，刷新列表。**多选时对全部选中文件批量应用**。

### 8.2 新建文件夹/重命名对话框

**弹出位置**：屏幕中央。
**内容**：单行文本输入框（带标签式浮动提示），弹出时自动弹出软键盘。
**行为**：输入非空校验；重命名时预填当前名并选中基础名部分；回车或点"确定"确认；重名/空名提示。

### 8.3 确认删除对话框

**弹出位置**：屏幕中央（纯文本提示，无表单）。
**触发**：多选「删除」或单文件菜单「删除」。
**内容（动态消息，按文件数与类型）：**
```
单个文件：
  · 目录："确定删除文件夹 {名称}？"
  · 文件："确定删除文件 {名称}？"
多个文件：
  · 全是目录："确定删除 {N} 个文件夹？"
  · 全是文件："确定删除 {N} 个文件？"
  · 混合："确定删除 {N} 项？"
按钮：取消 / 确定
```
**确定** → 后台执行删除，Toast"开始删除"。

### 8.4 文件属性对话框（多 Tab，第 14 节详述）

**弹出位置**：屏幕中央，全屏宽度对话框。
**触发**：单文件菜单「属性」或详情入口。
**结构**：顶部 Tab 标签页 + 内容切换区，按文件类型动态显示可用页签（基本/标签/权限/校验和/图片/音频/视频/APK）。

### 8.5 文件详情 / 预览对话框（新增，标签式）

**弹出位置**：屏幕中央。
**触发**：单击文件行标签片或详情入口。
**内容**：文件信息卡（名称/大小/类型/导入时间）+ 标签卡（可编辑，等同打标签）。

### 8.6 创建压缩包对话框 / 压缩包密码对话框

**创建压缩包**：屏幕中央，字段含压缩包名、格式（ZIP/7Z/TAR 单选）、压缩级别、密码。触发：多选「压缩」或单文件菜单「压缩」。
**压缩包密码**：屏幕中央，密码输入框。触发：访问加密压缩包需密码时。

### 8.7 文件冲突 / 错误对话框（后台作业）

**冲突**：屏幕中央，选项"合并或替换 / 重命名 / 跳过"。触发：复制/移动遇同名文件。
**错误**：屏幕中央，显示错误信息 + 重试/跳过/取消。触发：后台文件操作失败。

### 8.8 导入进度（前台服务通知 / Toast）

**位置**：通知栏（前台服务 `ForegroundNotificationManager`）+ 底部 Toast。
**内容**：进度条 + "正在导入 N 个文件"；完成 Toast"已导入 N 个文件，已打上标签 [默认标签]"。

### 8.9 权限请求引导对话框

**触发**：首次进入库请求存储/通知权限；拒绝后再次触发时弹"去设置开启"引导。
**弹出位置**：屏幕中央。
**内容**：说明 + "去设置 / 取消"。

### 8.10 全部对话框弹出位置速查表

| 对话框 | 弹出位置 | 类型 | 触发 |
|--------|----------|------|------|
| 打标签 | 屏幕中央 | 多选 Chip + 输入 | 多选/单选「打标签」|
| 新建文件夹/重命名 | 屏幕中央 | 单输入框 | 菜单「新建/重命名」|
| 确认删除 | 屏幕中央 | 纯文本 | 多选/单选「删除」|
| 文件属性 | 屏幕中央(全宽) | 多 Tab | 菜单「属性」|
| 文件详情 | 屏幕中央 | 信息+标签卡 | 点标签/详情 |
| 创建归档 | 屏幕中央 | 表单 | 菜单「压缩」|
| 归档密码 | 屏幕中央 | 密码框 | 打开加密归档 |
| 文件冲突 | 屏幕中央 | 三选一 | 后台作业冲突 |
| 文件错误 | 屏幕中央 | 错误+按钮 | 后台作业失败 |
| 权限引导 | 屏幕中央 | 文本+去设置 | 权限被拒 |

---

## 9. 导入 / 导出功能设计

### 9.1 导入流程（系统文件选择器）

**触发**：点击工具栏**右2「导入」按钮**（常驻显示）。

**步骤详解（用户视角）：**
```
点击「导入」
  → 弹出系统文件选择器（全屏），支持多选，仅显示本地文件
  → 用户选择一个或多个文件
  → 系统向应用授予这些文件的读取权限
  → 逐个将文件内容复制进应用私有库目录
       · 以随机文件名保存，避免与库内文件重名冲突
       · 同时记录：文件名、大小、类型、导入时间、库内位置
       · 每个文件自动打上「默认标签」(见 10.2)
  → 库列表刷新
  → 提示"已导入 N 个文件"（大文件用通知栏进度条）
```

**失败处理**：单文件读取失败 → 跳过该文件并记录错误；全部失败 → 错误提示；部分成功 → "成功导入 N 个，失败 M 个"。

### 9.2 导出流程（保存到系统位置）

**触发**：用户选中文件后点击工具栏**右3「导出」**（或多选菜单「导出」）。

**步骤详解（用户视角）：**
```
用户选中一个或多个文件
  → 点击「导出」
  → 若没有任何选中：
       提示"请先选择要导出的文件"
       （可选：自动进入多选模式，提示用户长按选择）
  → 弹出系统保存界面：
       单文件 → 指定保存文件名和位置
       多文件 → 选择目标文件夹
  → 用户确认保存位置
  → 把应用库内文件复制到该位置
  → 提示"已导出 N 个文件"
```

**导出范围**：只能导出**已选中**文件；未选中时引导用户先多选。

### 9.3 导入/导出按钮状态规则

| 按钮 | 无选中时 | 有选中时 |
|------|----------|----------|
| 导入（右2） | 恒可用，直接打开选择器 | 恒可用（导入与选中无关）|
| 导出（右3） | 点击提示"请先选择"，或进入多选模式 | 导出全部选中文件 |

### 9.4 导入式存储与权限模型

| 项 | 设计 |
|----|------|
| 库文件存放 | 应用私有目录（无需任何存储权限即可读写）|
| 读取系统文件 | 仅通过系统文件选择器逐个授权，无全局存储权限 |
| 写入系统位置 | 仅通过系统保存界面由用户显式授权 |
| 库索引 | 记录在库数据库中，不依赖文件系统实时扫描 |
| 隔离性 | 应用卸载即库数据消失（如需持久可考虑公开目录）|

> **核心原则**：本设计不需要"所有文件访问"全局权限，所有对系统文件的读写都由用户在系统选择器中**显式授权**，更安全也符合新版系统权限模型。

---

## 10. 标签系统设计（核心新增）

### 10.1 数据模型
```
LibraryFile (库文件记录)
  ├─ id: 编号
  ├─ name: 文件名
  ├─ size: 大小
  ├─ mimeType: 文件类型
  ├─ importedAt: 导入时间
  ├─ pathInLibrary: 库内相对位置
  └─ tags: 标签集合

Tag (标签)
  ├─ name: 标签名 (唯一)
  └─ color: 标签颜色 (可选，用于显示)
  ├─ pathInLibrary: String   (应用私有相对路径)
  └─ tags: MutableSet<String>  ← 标签集合

Tag (标签)
  ├─ name: String (主键/唯一)
  └─ color: Int (可选，用于 chip 显示)
```

### 10.2 默认标签规则
- 每个导入的文件在导入成功时**自动打上默认标签**「已导入」。
- 也可按导入来源分类：如"从相册导入"、"从文件导入"，自动打对应标签。

### 10.3 打标签入口（三种）
| 方式 | 触发 | 作用对象 |
|------|------|----------|
| 多选打标签 | 多选工具栏「打标签」 | 全部选中文件 |
| 单选打标签 | 单文件菜单「打标签」 | 单个文件 |
| 详情编辑 | 文件详情/标签卡 | 单个文件 |

### 10.3b 批量打标签的完整流程（多选场景）

```
多选模式 → 点击多选工具栏「打标签」
  → 弹出打标签对话框（作用对象 = 全部选中文件，标题"为 N 个文件打标签"）
  → 用户勾选已有标签 / 新建标签
  → 点击"确定"
  → 对所有选中文件批量应用新标签集合
       → 库列表刷新（受影响行局部更新）
       → 多选状态保留（不清除选中），用户可继续打标签或导出
  → 提示"已为 N 个文件打上标签"
```

**关键点**：
- 批量更新**要么全部成功要么全部回滚**，保证一致性。
- 更新后**保留多选状态**，方便连续操作。
- 单个文件走同一对话框（N=1），逻辑复用。

### 10.4 标签显示
- 列表项在名称/描述下方新增一行标签片（chips），显示该文件的所有标签。
- 默认标签"已导入"用图标区右下角徽章标识。
- 标签用彩色标签片区分，颜色在标签管理中设定。

### 10.5 标签管理页
模式 A 页面（顶部栏 + 内容区），列表展示全部标签：
- 每个标签行：标签名 + 颜色 + 文件数量 + 重命名/删除。
- 提供"新建标签"入口。

### 10.6 标签删除行为
删除标签时：确认对话框提示"将从 N 个文件中移除该标签"；执行后所有引用该标签的文件同步更新。

---

## 11. 标签搜索功能设计

### 11.1 触发入口
- 顶部工具栏溢出菜单「标签搜索」，或导航抽屉标签区。
- 也可在文件库内容区顶部常驻搜索条。

### 11.2 页面布局（模式 F）
```
顶部栏 (title="标签搜索")
  └─ 内容区 (垂直排列)
       ├─ 搜索框 (提示"搜索文件名或标签")
       ├─ 一排横向滚动的标签筛选片 (可多选，列出全部标签)
       └─ 文件结果列表 (实时过滤)
```

### 11.3 过滤逻辑（组合条件）
- **文件名搜索**：输入关键词匹配文件名。
- **标签过滤**：勾选一个或多个标签筛选片。
- **组合**：文件名 AND 标签（AND 组合）或 OR 组合（可在设置选择）。
- 结果为空显示"未找到匹配文件"。

### 11.3b 标签搜索实现与性能

**数据查询原则（数据库驱动而非文件扫描）：**
- 按文件名模糊匹配关键词。
- 按选中标签过滤。
- AND 组合：文件需匹配全部选中的标签；OR 组合：匹配任一即可。
- 按导入时间倒序展示。

**性能表现**：
- 文件名与标签均建立索引，保证搜索/过滤即时响应。
- 标签关联做唯一化处理，避免重复。
- 大结果集采用**分页加载**，滚动时增量加载，避免一次性加载卡顿。
- 输入**防抖**：停止输入 300ms 后才发起查询。
- 列表**实时刷新**：标签/文件数据变化时结果自动更新。

### 11.4 结果交互
- 结果列表项复用文件列表项布局。
- 点击打开查看器；长按进入多选（可继续打标签/导出）。
- 实时刷新：每次输入/勾选立即更新结果。
- 结果行同样支持标签片显示。

---

## 11b. 内置文件查看器详解（模式 C / 模式 A）

> 所有查看器是独立页面挂在各自 Activity 中。打开时从文件库读取（读取应用私有库的副本，而非原始系统路径）。点击文件行时按类型分发到对应查看器。
>
> **打开文件分发规则**：
> ```
> APK      → 打开方式对话框(安装/查看)
> PDF      → PDF 查看器
> EPUB/MOBI → 电子书查看器
> CSV      → CSV 查看器
> 图片      → 图片查看器
> 视频      → 视频播放器
> 音频      → 音频播放器
> 文本      → 文本编辑器
> 文件夹/压缩包 → 进入/浏览
> 其他      → 交给系统打开
> ```
> 视频      → VideoViewer
> 音频      → AudioPlayer
> 文本      → TextEditor
> 文件夹/归档 → 进入/浏览
> 其他      → 系统 Intent 打开
> ```

### 11b.1 图片查看器 —— 模式 C

```
整屏叠层
  ├─ 多图横向滑动区   ← 整屏，左右滑动切图
  └─ 顶部浮层 AppBar (背景=栏面色)
       └─ 工具栏 (?actionBarSize)
```
- 手势缩放（单指平移、双指捏合缩放，支持大图分块加载）。
- 每页一张图片，滑动切换上一张/下一张。
- 从列表打开时传入当前目录下**相邻图片**（受 1000 张上限保护，居中定位）。
- 顶部浮层工具栏：标题 = 文件名，含菜单（删除/分享/属性等）。

### 11b.2 视频播放器 —— 模式 C（media3 ExoPlayer）

```
整屏叠层 (背景=媒体背景色)
  ├─ 播放画面 (PlayerView, 自适应缩放)
  ├─ 居中圆形进度条 (缓冲时)
  ├─ "2.0x" 倍速文字 (居中, 默认隐藏)  ← 长按倍速时显示
  ├─ 顶部浮层 (渐变压暗遮罩) └─ 工具栏
  └─ 底部进度条区 (底部渐变遮罩, 默认隐藏)
       ├─ 进度滑杆 (thumbRadius=6dp, trackHeight=3dp)
       └─ 时间行 (横向)
            ├─ 当前时间 (等宽字体, LabelSmall)
            ├─ 占位留白 (weight=1)
            └─ 总时长 (等宽字体, 较浅色)
```
**手势：**
- **横向滑动**：快进/快退（滑动距离映射到进度）。
- **长按**：循环倍速（1x → 1.5x → 2x），松开恢复 1x，居中显示当前倍速文字。
- **点击**：显示/隐藏顶部工具栏与底部进度条。
- **底部进度滑杆**：拖动跳转进度，左右显示当前/总时长（`00:00` 格式）。
- 顶部渐变遮罩保证工具栏在亮背景下可读。

### 11b.3 音频播放器 —— 模式 C

```
整屏叠层 (背景=媒体背景色)
  ├─ 专辑封面 (整屏, 内边距64dp, 居中显示, 无封面用默认音频图标)
  ├─ 居中圆形进度条 (默认隐藏)
  ├─ "2.0x" 倍速文字 (居中, 默认隐藏)
  ├─ 顶部浮层 (渐变压暗遮罩) └─ 工具栏
  └─ 底部进度条区 (底部渐变遮罩, 默认隐藏)
       ├─ 进度滑杆 (0..1000)
       └─ 时间行 (当前/总时长)
```
- **专辑封面**：铺满全屏（从音频文件提取内嵌封面，无封面用默认音频图标）。
- 播放控制：点击暂停/播放、左右滑切歌、长按倍速。
- 底部进度条拖动。

### 11b.4 PDF 查看器 —— 模式 C

```
整屏叠层
  ├─ 页面滚动区 (逐页渲染, 背景=浅色容器)
  ├─ 居中内容 (加载中)
  │    ├─ 圆形进度条
  │    └─ 文件大小文字
  ├─ 居中错误提示 (图标+文字, 默认隐藏)
  ├─ 顶部浮层 (背景=栏面色) └─ 工具栏
  └─ 底部居中小卡片 (页码指示, 圆角16dp, 阴影4dp)
       └─ "第 X / Y 页" (LabelMedium, 默认隐藏)
```
- **逐页渲染**：滚动区每项一页，滚动时按需加载。
- **页码指示**：底部居中卡片显示当前页/总页数。

### 11b.5 文本编辑器 —— 模式 A（顶部栏+内容区）

```
顶部栏 (背景=栏面色, 滚动时浮起)
  └─ 工具栏
内容区
  ├─ 居中圆形进度条 (默认隐藏)
  ├─ 居中错误提示 (默认隐藏)
  └─ 快速滚动文本编辑区 (充满, 自动换行)
       └─ 多行文本输入 (可滚动)
```
**工具栏菜单：**
| 项 | 显示 | 行为 |
|----|------|------|
| 保存 | 常驻图标 | 保存修改 |
| 重新加载 | 溢出 | 重新读取文件（未保存则弹确认）|
| 编码 | 溢出子菜单 | 切换文件编码（自动检测当前）|

**行为**：编码自动检测 + 手动切换；退出/重新加载时若未保存弹"确认放弃修改"对话框；支持大文本滚动编辑。

### 11b.6 大文本查看器 —— 模式 A
顶部栏 + AppBar + 快速滚动文本区 + 只读文本视图，**分行懒加载**渲染超大文本，支持搜索定位。

### 11b.7 十六进制查看器 —— 模式 B 变体（工具宿主内）

```
竖排布局
  ├─ 文件信息文字 (默认隐藏)
  ├─ 垂直滚动区 (占满剩余空间)
  │    └─ 水平滚动区
  │         └─ 十六进制内容 (等宽字体 12sp, 内边距16dp)
  └─ "加载更多"文字按钮 (默认隐藏)
```
- **Hex + ASCII 并排**显示二进制（等宽字体 12sp）。
- 大文件"加载更多"分页加载；横竖均可滚动。

### 11b.8 CSV 查看器 —— 模式 C（网页渲染）

```
整屏叠层
  ├─ 网页渲染区 (默认隐藏)  ← 渲染 CSV 表格
  ├─ 居中圆形进度条 (默认隐藏)
  ├─ 居中错误提示 (默认隐藏)
  └─ 顶部浮层 (背景=栏面色) └─ 工具栏
```
CSV 解析后生成 HTML 表格由网页渲染区显示，支持横向/纵向滚动。

### 11b.9 电子书查看器（EPUB/MOBI）—— 模式 A 变体
顶部工具栏 + 网页渲染区（整页渲染文本）。解析章节，分页/翻页阅读。

### 11b.10 查看器弹出位置与菜单速查

| 查看器 | 宿主布局 | 顶部浮层 | 菜单 |
|--------|----------|----------|------|
| 图片 | 整屏叠层 | 浮层 | 删除/分享/属性 |
| 视频 | 整屏叠层 + 渐变 | 浮层(渐变) | 空 |
| 音频 | 整屏叠层 + 渐变 | 浮层(渐变) | 空 |
| PDF | 整屏叠层 | 浮层 | 空 |
| 文本 | 顶部栏+内容区 | 顶部栏 | 保存/重载/编码 |
| 大文本 | 顶部栏+内容区 | 顶部栏 | 搜索/编码 |
| Hex | 工具宿主内竖排 | 工具顶部栏 | — |
| CSV | 整屏叠层 | 浮层 | 空 |
| 电子书 | 顶部栏+网页区 | 顶部栏 | 目录/设置 |

---

## 12. 侧边抽屉导航

### 12.1 结构
侧边抽屉是一个靠左展开的窄条面板（宽度自适应内容，最大不超过屏幕 1/3），内部为可上下滚动的导航列表，带细滚动条。

### 12.2 导航项类型
每个导航项是一行：
```
[图标] 标题
        副标题(可省略, 自动隐藏)
```
- 行高约 56dp，图标在左，标题 + 可选副标题在右。
- 项与项之间可用分隔线分组。
- 当前所在位置对应的项会高亮显示。

### 12.3 导航项顺序（导入式改造）

| 分组 | 项 | 行为 |
|------|-----|------|
| 库 | 全部文件 | 显示库全部 |
| 库 | 收藏 | 显示打"收藏"标签的文件 |
| 库 | 最近导入 | 按导入时间倒序 |
| **标签区** | 全部标签（标题） | 不可点，仅标题 |
| 标签 | 各标签快捷项（动态） | 点击 → 按该标签过滤库 |
| 回收站 | 回收站 | 进入回收站页 |
| 菜单 | 设置 | 打开设置页 |
| 菜单 | 显示设置 | 打开显示设置页 |
| 菜单 | 标签管理 | 打开标签管理页 |
| 菜单 | 关于 | 打开关于页 |

### 12.4 交互
- 点击：过滤/跳转 + 关闭抽屉。
- 长按标签项：编辑标签。
- 从抽屉启动页返回时恢复抽屉打开状态。

### 12.5 抽屉打开方式
- 点击顶部工具栏最左"三横线"图标 → 抽屉从左侧滑出。
- 从屏幕左边缘向右滑动 → 抽屉滑出（边缘手势）。
- 抽屉滑出时背景轻微变暗；点击抽屉外区域或再次左滑 → 收起。

### 12.6 抽屉内导航项的行为细节
- **存储/库项**：显示当前项对应的副标题（如剩余/总空间）。点击进入对应根视图，当前项高亮。
- **标签快捷项**：点击后文件库按该标签过滤，抽屉该项高亮。
- **菜单项（设置/显示设置/标签管理/关于）**：点击打开对应页面，返回时抽屉恢复打开。

---

## 13. 设置页 / 显示设置页 / 标签设置页

### 13.1 设置页（模式 A）
顶部栏 + 内容区，内容区为竖排的卡片式设置项列表。每项是一个卡片：**标题 + 副标题(当前值摘要)**，点击弹出对话框或进入子页。

| 设置项 | 类型 | 选项 |
|--------|------|------|
| 语言 | 单选 | 跟随系统 / 中文 / English |
| 默认标签名 | 文本 | 导入时自动打上的标签名（默认"已导入"）|
| 标签搜索逻辑 | 单选 | AND / OR |
| 文件名显示 | 单选 | 省略方式 |
| 存储位置 | 子页 | 库存储目录设置 |
| 标签管理 | 子页 | 进入标签管理页 |

### 13.2 显示设置页（模式 A，滑杆列表）

**布局结构：**
```
顶部栏 (title="显示设置", 带返回箭头)
  └─ 内容区 (竖排列表)
       ├─ [预设行] 可点击卡片 → 弹出预设选择对话框
       ├─ [滑杆组1] 字体大小
       ├─ [滑杆组2] 界面间距
       ├─ [滑杆组3] 列表项高度
       ├─ [滑杆组4] 图标大小
       ├─ [滑杆组5] 页面边距
       ├─ [滑杆组6] 对话框内边距
       ├─ [滑杆组7] 按钮间距
       └─ [滑杆组8] 背景遮罩强度
```

**预设行**：标题"预设"，点击弹出对话框，选项为**紧凑 / 默认 / 宽松**（本设计可加"自定义"）。选择后所有滑杆立即跳转到对应预设值，界面实时变化。

**每个滑杆行**（竖排）：
```
[左：标题    右：当前值(如 100%)]
[滑杆 —— 填充条 ——]
```
- 标题左对齐，当前值右对齐且用强调色。
- 滑杆范围 50% ~ 200%，步进 5%。
- 拖动滑杆 → 右侧百分比实时更新，界面立即缩放预览。
- 背景遮罩强度滑杆范围 0% ~ 100%，步进 5%（控制浮层弹出时背景变暗程度）。

**实时生效**：任何滑杆或预设改变，立即广播"界面设置已变更"，各页面自动刷新，无需重启。

### 13.3 标签管理页（模式 A，第 10.5 节）

**布局结构：**
```
顶部栏 (title="标签管理", 带返回箭头 + 右上"新建标签"按钮)
  └─ 内容区 (竖排列表)
       └─ 每个标签一行：
            [标签色圆点] 标签名   文件数量    [更多]菜单
```

**标签行交互**：
- 点击标签行 → 打开"编辑标签"对话框（改名 / 改颜色）。
- 行尾"更多"菜单 → 删除标签（弹确认，提示"将从 N 个文件移除"）。
- 右上"新建标签" → 弹"新建标签"对话框（输入名称 + 选颜色）。

**空状态**：无任何标签时显示"暂无标签，点击右上角新建"。

---

## 13b. 辅助页面详解（回收站 / 关于 / 存储）

### 13b.1 回收站页（模式 A）

**布局结构：**
```
顶部栏 (title="回收站", 带返回箭头)
  └─ 内容区
       ├─ 文件列表 (每行: 图标 + 文件名 + 删除时间, 可多选)
       └─ 空状态 (居中: 垃圾桶图标 + "回收站为空")
右下角浮动按钮：清空回收站 (垃圾桶图标)
```

**交互**：
- 列表项**长按进入多选**，多选工具栏出现（恢复 / 彻底删除）。
- 右下角浮动按钮"清空" → 确认对话框 → 彻底清空。
- 空状态时浮动按钮隐藏。

**多选菜单**：恢复（还原到原库）、彻底删除。

### 13b.2 关于页（模式 A）

**布局结构：**
```
顶部栏 (title="关于", 带返回箭头)
  └─ 内容区 (可滚动, 卡片堆叠)
       ├─ [应用信息卡] 居中
       │    ├─ 应用图标 (居中, 72dp)
       │    ├─ 应用名称 (HeadlineSmall, 加粗)
       │    └─ 版本号 (BodyMedium, 较浅色)
       ├─ [许可证卡] 单行: [文档图标] 开源许可证 → 点击打开
       ├─ [作者卡]
       │    ├─ 分组标题 "作者"
       │    ├─ 行1: [人像图标] 作者名称
       │    └─ 行2: [GitHub图标] GitHub 链接
       └─ [隐私/其他] (可选)
```

### 13b.3 存储位置页（设置→存储，模式 A）

**布局结构：**
```
顶部栏 (title="存储", 带返回箭头 + 右上"添加")
  └─ 内容区 (竖排列表)
       └─ 每行: [存储图标] 名称  描述(剩余/总空间)   [拖动手柄]
```

**交互**：
- 行尾拖动手柄可**上下拖拽调整顺序**。
- 右上"添加" → 添加系统位置（SAF 目录授权）。
- 行尾"更多"菜单 → 编辑 / 移除。

---

## 14. 文件属性与文件详情

### 14.1 文件属性对话框（多页签）
```
屏幕中央对话框 → 顶部页签 + 内容切换区
  按文件类型动态显示可用页签
  ├─ 基本属性（恒显示）
  ├─ 标签（恒显示，本设计新增）
  ├─ 权限（Linux/Root 场景）
  ├─ 校验和（恒显示）
  ├─ 图片信息（仅图片）
  ├─ 音频元数据（仅音频）
  ├─ 视频元数据（仅视频）
  └─ APK 信息（仅 APK）
```

### 14.1b 各 Tab 字段清单

**基本属性 Tab**
| 字段 | 内容 |
|------|------|
| 名称 | 文件名 |
| 大小 | 人类可读大小 + 字节数 |
| 类型 | MIME 类型 + 图标 |
| 导入时间 | 入库时间（本设计特有）|
| 库路径 | 应用私有相对路径（本设计特有）|
| 修改时间 | 最后修改时间 |

**标签 Tab**
- 显示当前文件全部标签（chips）。
- 提供「编辑标签」→ 打开打标签对话框（作用该文件）。

**权限 Tab（Linux/Root 场景）**
- Owner / Group（可改）。
- Mode 逐位（rwx，`mode_bit_item`）。
- SELinux 上下文。
- 子对话框：设置 Owner/Group、设置 Mode、设置 SELinux 上下文。

**校验和 Tab**
- MD5 / SHA-1 / SHA-256 / SHA-512 / CRC32 逐项计算显示，复制按钮。

**图片信息 Tab**
- 尺寸、分辨率、文件类型、相机制作（EXIF）、GPS 坐标、拍摄时间。

**音频元数据 Tab**
- 标题、艺术家、专辑、时长、比特率、采样率、轨道号。

**视频元数据 Tab**
- 编码、分辨率、帧率、时长、比特率、音轨信息。

**APK 信息 Tab**
- 包名、版本名/码、最小 SDK、权限列表（`permission_list_dialog`）、签名。

### 14.2 触发位置
- 单文件菜单「属性」/ 详情入口。
- 弹出位置：屏幕中央全屏宽度对话框。
- 各页签数据按需后台加载（每页签独立加载，随文件变化自动更新）。

---

## 15. 数据模型与状态管理

### 15.1 数据存储结构

```
库文件记录：编号、文件名、大小、文件类型、导入时间、库内相对位置
文件-标签关联：文件编号 ↔ 标签名   (一个文件可有多个标签，多对多)
标签：标签名(唯一)、标签颜色
```

### 15.2 界面状态来源

界面所需的数据状态包括：
- 当前视图的文件列表（含加载中/空/错误三种状态）。
- 当前选中的文件集合（多选状态）。
- 标签筛选条件（选中的标签）。
- 搜索关键词。
- 全部标签列表。
任何状态变化都会驱动界面局部刷新，无需手动刷新整页。

### 15.3 选择状态管理
- 选中集合：保持选择顺序、自动去重。
- 操作：单个增/删、批量增/删、整体替换（全选）、清空。
- 选择变化 → 界面自动更新多选工具栏的显隐与按钮内容。

### 15.4 批量操作交互冲突处理

| 场景 | 处理规则 |
|------|----------|
| 打标签后 | **保留选中**，可继续打标签/导出/分享 |
| 导出后 | 保留选中，导出完成提示 |
| 删除后 | **清除选中**，多选工具栏淡出 |
| 全选后点击单个 | 取消该单个选中，其余保留 |
| 操作中再次导入 | 导入不改变选中状态 |
| 空选中点导出 | 提示，不进入导出 |

**核心原则**：
- 删除 = 结束性操作 → 成功后清空选中。
- 打标签/导出 = 非破坏性操作 → 成功后保留选中。
- 所有批量操作对选中集合整体生效，成功后按上述规则更新选中状态。

### 15.5 状态流转总览
```
普通浏览（常规工具栏）
   │ 长按/勾选
   ▼
多选模式（多选工具栏，有选中文件）
   │  打标签 → 保留选中 → 标签对话框
   │  导出   → 保留选中 → 系统保存界面
   │  删除   → 清空选中 → 确认删除对话框
   │  分享   → 保留选中 → 系统分享
   │  全选   → 选中全部可见文件
   ▼
取消(×/返回) → 清空选中 → 回到普通浏览
```

---

## 16. 复刻实现要点清单

### 第一步：设计令牌与布局模板
1. 建立间距/字体/卡片/列表/图标令牌（第 3 章）。
2. 启用 Material You 动态取色 + 夜间模式。
3. 建立 6 种布局模式模板（第 4 章）。

### 第二步：核心文件库界面
4. 实现"抽屉 + 持久底部栏 + 顶部栏"整体框架。
5. 实现顶部双工具栏（常规 + 多选）+ 面包屑导航。
6. **在工具栏加入导入/导出两个按钮**（右2、右3）。
7. 实现列表/网格双视图 + 标签片行。
8. **精确复刻交互机制**（第 6 章）：单击/长按/图标点击/菜单按钮的分支逻辑。

### 第三步：菜单与对话框
9. 实现单文件操作菜单 + 多选菜单 + 视图排序子菜单（第 7 章）。
10. 实现打标签对话框 + 确认删除 + 属性对话框（第 8 章）。

### 第四步：导入导出与标签系统
11. 实现系统选择器导入流程 + 自动默认标签。
12. 实现系统保存界面导出流程。
13. 实现标签数据模型 + 单选/多选打标签 + 标签管理页。
14. 实现标签搜索页（搜索框 + 标签筛选片过滤 + 分页/防抖优化）。

### 第五步：其余功能
15. 实现侧边抽屉（库/标签区/回收站/菜单）。
16. 实现设置/显示设置/标签管理页。
17. 实现内置查看器与文件属性多页签对话框。
18. 校验交互优先级与批量操作冲突规则（6.11、15.4）。

---

> **说明**：本文档基于参考工程 AdvanceFileManager 的布局与交互，提取了设计令牌、6 类布局模式、单击/长按/单选/多选的精确交互规则、每个菜单与对话框的**弹出位置和内容**，并在此基础上改造为**导入式文件管理器**，新增**导入/导出双按钮**、**默认标签**、**单选/多选打标签**与**标签搜索**。复刻时先建立第 3 章令牌与第 4 章模板，再按第 6 章精确实现交互，即可获得一致且完整的页面与功能布局。本文档仅描述界面布局与交互，不含代码实现。
