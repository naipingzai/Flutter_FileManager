# Flutter File Manager — 工程接口与文件打开流程文档

> 整理日期：2026-08-11
> 架构：Flutter (Dart) + C++ 原生库 (FFI)，无 Dart 原生文件操作回退，C++ 是唯一文件操作来源。
>
> **实现状态：**
> - ✅ 文件打开用 APP 内部组件、不依赖系统能力：已编码实现，编译通过
> - ✅ 图片解码：C++ 集成 stb_image 单头文件源码，RGBA 输出经 FFI 渲染
> - ✅ 电子书：C++ 集成 miniz 单文件源码，解包 EPUB 提取正文
> - ⏳ 视频/音频：已搭好 media_codec 接口与 FFmpeg 条件编译骨架（HAVE_FFMPEG），FFmpeg 全源码集成（下载/configure/NDK 交叉编译）为后续大工程

---

## 一、工程架构总览

```
┌─────────────────────────────────────────────┐
│               Flutter UI 层 (Dart)           │
│  lib/screens/      页面（主界面、搜索、设置） │
│  lib/widgets/      列表/网格/面包屑组件       │
│  lib/providers/    FileManagerState 状态管理  │
│  lib/services/     FileService 服务封装       │
│  lib/native/       FFI 绑定层                 │
└───────────────┬─────────────────────────────┘
                │  dart:ffi 调用
                ▼
┌─────────────────────────────────────────────┐
│            FFI 绑定层 (Dart)                 │
│  lib/native/file_ops_bindings.dart          │
│  全平台加载原生库（DynamicLibrary.process）  │
└───────────────┬─────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────┐
│     C++ 原生层（全平台、内部集成、无系统库）   │
│  native/file_ops.h / .cpp     核心文件操作    │
│  native/file_ops_json.h / .cpp  JSON 封装    │
└─────────────────────────────────────────────┘
```

**调用链（以列目录为例）：**
```
FileManagerState.loadCurrentTab()
  → FileService.listDirectory(path)
    → FileOpsNative.listDirectory (FFI)
      → file_ops_json_list_directory (C++ JSON 封装)
        → file_ops_list_directory (C++ 核心实现，内部集成，不依赖系统库)
```

---

## 二、C++ 原生层接口（native/）

### 2.1 file_ops.h — 核心文件操作（C 导出）

**数据结构：**
| 结构体 | 用途 |
|--------|------|
| `FileInfo` | 单个文件/目录信息（名称、路径、类型、大小、时间、权限、属主、MIME、读写执行标志、隐藏标志） |
| `DirListResult` | 目录列表结果（数组 + 数量 + 错误信息） |
| `SearchResult` / `SearchResultList` | 搜索结果（路径、名称、类型、大小、修改时间） |
| `HashResult` | 哈希结果（MD5 / SHA1 / SHA256 / SHA512 / CRC32） |
| `DiskUsageInfo` | 磁盘用量（总量 / 剩余 / 已用） |

**枚举 `FileType`：**
`UNKNOWN(0), REGULAR(1), DIRECTORY(2), SYMLINK(3), SOCKET(4), FIFO(5), BLOCK_DEVICE(6), CHAR_DEVICE(7)`

**核心接口列表：**

| 函数 | 功能 |
|------|------|
| `file_ops_list_directory(path, show_hidden)` | 列出目录内容 |
| `file_ops_get_file_info(path)` | 获取单个文件/目录的详细信息 |
| `file_ops_create_directory(path, err, err_size)` | 创建目录 |
| `file_ops_create_file(path, err, err_size)` | 创建空文件 |
| `file_ops_delete_file(path, err, err_size)` | 删除文件 |
| `file_ops_delete_directory(path, recursive, err, err_size)` | 删除目录（可选递归） |
| `file_ops_rename(old, new, err, err_size)` | 重命名/移动 |
| `file_ops_copy_file(src, dst, err, err_size)` | 复制文件 |
| `file_ops_move_file(src, dst, err, err_size)` | 移动文件 |
| `file_ops_exists(path)` | 判断路径是否存在 |
| `file_ops_is_directory(path)` | 判断是否为目录 |
| `file_ops_access(path, mode)` | 访问权限检查 |
| `file_ops_chmod(path, mode, err, err_size)` | 修改权限 |
| `file_ops_chown(path, uid, gid, err, err_size)` | 修改属主 |
| `file_ops_lchown(path, uid, gid, err, err_size)` | 修改符号链接属主 |
| `file_ops_symlink(target, linkpath, err, err_size)` | 创建符号链接 |
| `file_ops_link(old, new, err, err_size)` | 创建硬链接 |
| `file_ops_realpath(path)` | 解析绝对路径 |
| `file_ops_readlink(path)` | 读取符号链接目标 |
| `file_ops_search_files(dir, pattern, max)` | 通配符搜索文件 |
| `file_ops_compute_hash(path)` | 计算文件哈希（MD5/SHA1/SHA256/SHA512/CRC32） |
| `file_ops_get_disk_usage(path)` | 获取磁盘使用量 |
| `file_ops_get_mime_type(filename)` | 获取 MIME 类型 |
| `file_ops_get_home_dir()` | 获取用户主目录 |
| `file_ops_get_root_dir()` | 获取根目录 |
| `file_ops_find_duplicates(dir, max)` | 查找重复文件 |
| `file_ops_find_empty_files(dir, max)` | 查找空文件 |
| `file_ops_get_recent_files(dir, days, max)` | 查找最近修改的文件 |
| `file_ops_encrypt_file(src, dst, pwd, err, err_size)` | AES-256-CBC 加密文件 |
| `file_ops_decrypt_file(src, dst, pwd, err, err_size)` | AES-256-CBC 解密文件 |
| `file_ops_read_file_text(path)` | 读取整个文件为字符串 |
| `file_ops_write_file_text(path, content, err, err_size)` | 写入字符串到文件 |
| `file_ops_read_file_chunk(path, offset, len, out_len)` | 读取文件指定区间的原始字节 |
| `file_ops_read_file_bytes(path, out_len)` | 读取整个文件为字节数组 |

### 2.2 file_ops_json.h — JSON 封装层

所有函数返回 JSON 字符串，Dart 端通过 `jsonDecode` 解析。**调用后必须用 `file_ops_free_json()` 释放。**

| 函数 | 返回 JSON 结构 | 对应核心函数 |
|------|--------------|--------------|
| `file_ops_json_list_directory(path, show_hidden)` | `{"error":"","items":[FileInfo...]}` | 列目录 |
| `file_ops_json_get_file_info(path)` | `{"error":"","info":FileInfo}` | 获取文件信息 |
| `file_ops_json_search_files(dir, pattern, max)` | `{"error":"","items":[SearchResult...]}` | 搜索文件 |
| `file_ops_json_compute_hash(path)` | `{"error":"","md5":"","sha1":"","sha256":"","sha512":"","crc32":""}` | 计算哈希 |
| `file_ops_json_get_disk_usage(path)` | `{"error":"","totalSpace":n,"freeSpace":n,"usedSpace":n}` | 磁盘用量 |
| `file_ops_json_find_duplicates(dir, max)` | `{"error":"","items":[{path,name,size}...]}` | 查找重复文件 |
| `file_ops_json_find_empty_files(dir, max)` | `{"error":"","items":[SearchResult...]}` | 查找空文件 |
| `file_ops_json_get_home_dir()` | `{"path":"..."}` | 主目录 |
| `file_ops_json_get_root_dir()` | `{"path":"..."}` | 根目录 |
| `file_ops_json_create_directory(path, err, err_size)` | 返回 int (0=成功) | 创建目录 |
| `file_ops_json_create_file(path, err, err_size)` | 返回 int | 创建文件 |
| `file_ops_json_delete_file(path, err, err_size)` | 返回 int | 删除文件 |
| `file_ops_json_rename(old, new, err, err_size)` | 返回 int | 重命名 |
| `file_ops_json_copy_file(src, dst, err, err_size)` | 返回 int | 复制 |
| `file_ops_json_move_file(src, dst, err, err_size)` | 返回 int | 移动 |
| `file_ops_json_exists(path)` | 返回 int (0/1) | 判断存在 |
| `file_ops_json_is_directory(path)` | 返回 int (0/1) | 判断目录 |
| `file_ops_json_access(path, mode)` | 返回 int | 权限检查 |
| `file_ops_json_chown(path, uid, gid, err, err_size)` | 返回 int | 修改属主 |
| `file_ops_json_lchown(path, uid, gid, err, err_size)` | 返回 int | 修改符号链接属主 |
| `file_ops_json_symlink(target, linkpath, err, err_size)` | 返回 int | 创建符号链接 |
| `file_ops_json_link(old, new, err, err_size)` | 返回 int | 创建硬链接 |
| `file_ops_json_realpath(path)` | `{"path":"..."}` | 解析绝对路径 |
| `file_ops_json_readlink(path)` | `{"target":"..."}` | 读取符号链接 |
| `file_ops_json_get_recent_files(dir, days, max)` | `{"error":"","items":[SearchResult...]}` | 最近文件 |
| `file_ops_json_encrypt_file(src, dst, pwd, err, err_size)` | 返回 int | 加密 |
| `file_ops_json_decrypt_file(src, dst, pwd, err, err_size)` | 返回 int | 解密 |
| `file_ops_json_read_text_file(path)` | `{"error":"","text":"..."}` | 读取文本文件 |
| `file_ops_json_write_text_file(path, content, err, err_size)` | 返回 int | 写入文本 |
| `file_ops_json_read_csv_file(path)` | `{"error":"","rows":[[...]]}` | 读取 CSV |
| `file_ops_json_read_hex_chunk(path, offset, len)` | `{"error":"","hex":"..","ascii":"..","length":n}` | 读取十六进制块 |
| `file_ops_json_read_image_as_base64(path)` | `{"error":"","base64":"...","length":n}` | 图片转 Base64 |
| `file_ops_free_json(json)` | 释放 JSON 字符串 | 释放内存 |

---

## 三、Dart FFI 绑定层（lib/native/file_ops_bindings.dart）

`FileOpsNative` 单例类，**全平台加载原生库**，绑定上述所有 C 函数。

**加载方式（需修改）：** 去掉 `DynamicLibrary.open('libfile_ops.so')` 硬编码库名，改为使用 `DynamicLibrary.process()` 加载当前进程符号（由 Flutter 插件/构建系统统一注册原生库），实现跨平台（Linux/Windows/macOS/Android/iOS）统一加载。

```dart
// 原（仅 Linux）：
// if (Platform.isLinux) return DynamicLibrary.open('libfile_ops.so');
// throw UnsupportedError('Platform not supported');

// 改为（全平台）：
DynamicLibrary _loadLibrary() => DynamicLibrary.process();
```

**设计原则：** CPP 层所有接口均为全平台通用，功能实现不依赖任何系统库（不调用 xdg-open、shell、系统 API 等外部能力），全部功能在 CPP 内部集成实现。

绑定的字段（与 C 函数一一对应）：
- `listDirectory`, `getFileInfo`, `searchFiles`, `computeHash`, `getDiskUsage`
- `findDuplicates`, `findEmptyFiles`, `getHomeDir`, `getRootDir`, `freeJson`
- `createDirectory`, `createFile`, `deleteFile`, `rename`, `copyFile`, `moveFile`
- `exists`, `isDirectoryFn`, `getRecentFiles`, `encryptFile`, `decryptFile`
- `readTextFile`, `writeTextFile`, `readCsvFile`, `readHexChunk`, `readImageAsBase64`

> **注意：** 原生层有 `file_ops_json_access/chmod/chown/lchown/symlink/link/realpath/readlink` 等接口，但 **Dart FFI 绑定层尚未绑定这些函数**，如需使用需在 `FileOpsNative._bindFunctions()` 中补充绑定。

---

## 四、Dart 服务层（lib/services/file_service.dart）

`FileService` 单例，是所有 Dart 层文件操作的统一入口。

### 4.1 枚举类型

| 枚举 | 值 | 说明 |
|------|-----|------|
| `FileViewerType` | text/image/video/audio/pdf/csv/ebook/external/unknown | 文件查看器路由类型 |
| `FileType` | unknown(0)~charDevice(7) | 镜像 C++ FileType |
| `SortMode` | name/size/modified/type | 排序方式 |
| `ViewMode` | list/grid | 视图模式 |

### 4.2 数据模型

| 类 | 说明 |
|----|------|
| `FileEntry` | 文件/目录条目（含名称、路径、类型、大小、时间、权限、属主、MIME 等） |
| `FileSize` | 字节数与人类可读格式 |
| `DiskUsage` | 磁盘用量（总量/剩余/已用 + 百分比） |
| `FileHash` | MD5/SHA1/SHA256/SHA512/CRC32 |
| `DuplicateGroup` | 重复文件组 |
| `HexChunkResult` | 十六进制块（hex + ascii + length） |

### 4.3 服务方法

| 方法 | 对应 FFI | 功能 | 返回值 |
|------|---------|------|--------|
| `getHomeDirectory()` | `getHomeDir` | 获取主目录 | Future<String> |
| `getRootDirectory()` | —（硬编码） | 获取根目录 | String（固定 `/`） |
| `listDirectory(path, {showHidden})` | `listDirectory` | 列出目录 | List<FileEntry> |
| `getFileInfo(path)` | `getFileInfo` | 获取文件信息 | FileEntry? |
| `exists(path)` | `exists` | 判断存在 | bool |
| `isDirectory(path)` | `isDirectoryFn` | 判断目录 | bool |
| `createDirectory(path)` | `createDirectory` | 创建目录 | String?（错误信息） |
| `createFile(path)` | `createFile` | 创建文件 | String? |
| `deleteFile(path)` | `deleteFile` | 删除文件 | String? |
| `rename(old, new)` | `rename` | 重命名 | String? |
| `copyFile(src, dst)` | `copyFile` | 复制 | String? |
| `moveFile(src, dst)` | `moveFile` | 移动 | String? |
| `searchFiles(dir, pattern, {maxResults})` | `searchFiles` | 搜索 | List<FileEntry> |
| `computeHash(path)` | `computeHash` | 哈希 | FileHash? |
| `getDiskUsage(path)` | `getDiskUsage` | 磁盘用量 | DiskUsage |
| `findDuplicates(path)` | `findDuplicates` | 查找重复文件 | List<DuplicateGroup> |
| `findEmptyFiles(path)` | `findEmptyFiles` | 查找空文件 | List<FileEntry> |
| `getRecentFiles(path, {limit})` | `getRecentFiles` | 最近文件（7天内） | List<FileEntry> |
| `determineViewer(path)` | —（Dart 扩展名判断） | 判断文件查看器类型 | FileViewerType |
| `readTextFile(path)` | `readTextFile` | 读取文本 | String? |
| `writeTextFile(path, content)` | `writeTextFile` | 写入文本 | String? |
| `readCsvFile(path)` | `readCsvFile` | 读取 CSV | List<List<String>> |
| `readHexChunk(path, offset, length)` | `readHexChunk` | 读取十六进制块 | HexChunkResult? |
| `readImageAsBase64(path)` | `readImageAsBase64` | 图片转 Base64 | String? |
| `readVideoData(path)` | `readVideoData` | **读取视频文件数据**（新增） | Uint8List? |
| `readAudioData(path)` | `readAudioData` | **读取音频文件数据**（新增） | Uint8List? |
| `readPdfData(path)` | `readPdfData` | **读取 PDF 文件数据**（新增） | Uint8List? |
| `readEbookData(path)` | `readEbookData` | **读取电子书文件数据**（新增） | Uint8List? |
| `encryptFile(src, dst, password)` | `encryptFile` | 加密 | Future<String?> |
| `decryptFile(src, dst, password)` | `decryptFile` | 解密 | Future<String?> |

### 4.4 文件查看器类型判断（determineViewer）

按扩展名判断：
- **图片**：.jpg .jpeg .png .gif .bmp .webp .svg .ico .tiff .tif .heic .avif
- **视频**：.mp4 .avi .mkv .mov .wmv .flv .webm .m4v .3gp
- **音频**：.mp3 .wav .flac .aac .ogg .wma .m4a .opus
- **PDF**：.pdf
- **CSV**：.csv
- **电子书**：.epub
- **文本**：.txt .log .md .json .xml .yaml .yml .toml .ini .conf .css .js .py .rs .go .dart .sh .bat .c .cpp .h .hpp .java .kt .swift .html .htm
- **其他**：external

---

## 五、状态管理层（lib/providers/file_manager_state.dart）

`FileManagerState extends ChangeNotifier`，管理所有 UI 状态。

| 方法 | 功能 |
|------|------|
| `initialize()` | 初始化，获取主目录并加载 |
| `loadCurrentTab()` | 重新加载当前标签页目录 |
| `navigateTo(path)` | 导航到指定目录（记录历史） |
| `goBack()` / `goForward()` | 历史前进/后退 |
| `addTab(path)` / `closeTab(i)` / `switchTab(i)` | 标签页管理 |
| `toggleHidden()` | 切换显示隐藏文件 |
| `toggleDirectoriesFirst()` | 切换目录是否优先（重排当前列表） |
| `setSortMode(mode)` | 设置排序方式，再次点击切换升/降序 |
| `setViewMode(mode)` | 设置视图模式（列表/网格） |
| `toggleSelection(path)` / `selectAll()` / `clearSelection()` | 文件多选管理 |
| `setSearchQuery(query)` / `toggleSearch()` | 搜索框状态管理 |
| `filteredEntries` | 过滤后的条目（按搜索关键词过滤） |
| `addBookmark(name, path)` / `removeBookmark(i)` | 书签管理 |
| `setDrawerSection(section)` | 侧边栏分区切换 |
| `createDirectory/createFile/deleteFile/rename/copyFile/moveFile` | 直接透传调用 FileService |
| `exists/getFileInfo/computeHash/getDiskUsage` | 直接透传调用 FileService |
| `searchFiles/findDuplicates/findEmptyFiles` | 直接透传调用 FileService |

---

## 六、UI 页面层（lib/screens/）

| 页面文件 | 功能 |
|----------|------|
| `file_manager_page.dart` | **主界面**：标签页、面包屑、文件列表/网格、状态栏、侧边栏、菜单、新建/删除/重命名/属性对话框 |
| `search_page.dart` | 文件搜索页（glob 模式），点击目录可跳转 |
| `settings_page.dart` | 设置页（存储用量、平台信息） |
| `about_page.dart` | 关于页 |

### 6.1 主界面关键交互（file_manager_page.dart）

**文件项点击处理 `_handleTap(context, state, entry)`：**
```dart
void _handleTap(BuildContext context, FileManagerState state, FileEntry entry) {
  if (state.currentTab.selectedPaths.isNotEmpty) {
    // 有多选时：点击切换选中状态
    state.toggleSelection(entry.path);
  } else if (entry.isDirectory) {
    // 目录：进入该目录
    state.navigateTo(entry.path);
  } else {
    _openEntry(context, state, entry);
  }
  // ⚠️ 注意：当前对【普通文件】没有做任何处理！点击无响应。
}
```

**文件项长按**：`state.toggleSelection(e.path)` → 进入多选模式

**文件项右键菜单（列表模式 "更多" 按钮）`_showEntryMenu`：**
- 属性（PropertiesDialog）
- 重命名
- 删除

**文件操作对话框：**
- `_showRenameDialog` 重命名
- `_createItem` 新建文件夹/文件
- `_confirmDelete` 确认删除（多选或单个）

---

## 七、文件查看器页面（lib/screens/viewer/）

| 文件 | 类名 | 功能 | 数据来源 |
|------|------|------|----------|
| `text_editor_page.dart` | `TextEditorPage` | 文本编辑器（读取+保存） | `FileService().readTextFile` / `writeTextFile` |
| `image_viewer_page.dart` | `ImageViewerPage` | 图片查看器（分页浏览） | `FileService().readImageAsBase64` |
| `video_player_page.dart` | `VideoPlayerPage` | 视频播放器（APP 内部渲染） | `FileService().readVideoData`（新增） |
| `audio_player_page.dart` | `AudioPlayerPage` | 音频播放器（APP 内部渲染） | `FileService().readAudioData`（新增） |
| `pdf_viewer_page.dart` | `PdfViewerPage` | PDF 查看器（APP 内部渲染） | `FileService().readPdfData`（新增） |
| `csv_viewer_page.dart` | `CsvViewerPage` | CSV 查看器（表格展示） | `FileService().readCsvFile` |
| `ebook_viewer_page.dart` | `EbookViewerPage` | 电子书查看器（APP 内部渲染） | `FileService().readEbookData`（新增） |
| `viewer_index.dart` | — | 统一导出所有 viewer 页面 | — |

> **设计原则**：所有文件打开均使用 **APP 内部组件** 渲染，**不允许依赖系统能力**（不使用 xdg-open、系统 Intent 等外部程序）。数据统一由 CPP 层读取后交给 Dart 层进行内部渲染。

---

## 八、打开文件的设计原则（APP 内部组件，不依赖系统）

**核心原则：** 文件打开一律使用 **APP 内部组件** 渲染，**禁止依赖任何系统能力**（不使用 xdg-open、系统 Intent、外部程序等）。

数据读取统一由 CPP 层完成，Dart 层拿到数据后在 APP 内部组件中渲染。**现有 `openFile()`（依赖 `Process.run('xdg-open', ...)`）属于系统依赖实现，应废弃。**

---

## 九、如何添加「点击打开文件」功能（实现方案）

### 步骤 1：修改 `_handleTap`（file_manager_page.dart 第 683 行）

在 `else if (entry.isDirectory)` 之后，增加普通文件的打开逻辑：

```dart
void _handleTap(BuildContext context, FileManagerState state, FileEntry entry) {
  if (state.currentTab.selectedPaths.isNotEmpty) {
    state.toggleSelection(entry.path);
  } else if (entry.isDirectory) {
    state.navigateTo(entry.path);
  } else {
    // 新增：点击普通文件 → APP 内部组件打开
    _openEntry(context, state, entry);
  }
}
```

### 步骤 2：新增 `_openEntry` 方法，路由到 APP 内部 viewer 组件

```dart
void _openEntry(BuildContext context, FileManagerState state, FileEntry entry) {
  final type = state.fileService.determineViewer(entry.path);
  switch (type) {
    case FileViewerType.text:
      Navigator.push(context, MaterialPageRoute(
        builder: (_) => TextEditorPage(path: entry.path)));
      break;
    case FileViewerType.image:
      Navigator.push(context, MaterialPageRoute(
        builder: (_) => ImageViewerPage(initialPath: entry.path)));
      break;
    case FileViewerType.csv:
      Navigator.push(context, MaterialPageRoute(
        builder: (_) => CsvViewerPage(path: entry.path)));
      break;
    case FileViewerType.video:
      Navigator.push(context, MaterialPageRoute(
        builder: (_) => VideoPlayerPage(path: entry.path)));
      break;
    case FileViewerType.audio:
      Navigator.push(context, MaterialPageRoute(
        builder: (_) => AudioPlayerPage(path: entry.path)));
      break;
    case FileViewerType.pdf:
      Navigator.push(context, MaterialPageRoute(
        builder: (_) => PdfViewerPage(path: entry.path)));
      break;
    case FileViewerType.ebook:
      Navigator.push(context, MaterialPageRoute(
        builder: (_) => EbookViewerPage(path: entry.path)));
      break;
    default:
      // 未知类型：弹出"无法在应用内打开"提示，绝不调用系统程序
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(content: Text('无法在应用内打开: ${entry.name}')));
  }
}
```

> **注意**：需要在文件顶部 `import 'screens/viewer/viewer_index.dart';`

### 步骤 3：修改 `search_page.dart` 的文件点击

`search_page.dart` 第 99 行目前只处理目录跳转，文件点击无反应，同样参考上面逻辑加上 APP 内部打开处理。

---

## 十、新增文件数据读取接口（CPP 层 + 内部渲染）

要让视频、音频、PDF、电子书在 APP 内部渲染，需要先在 CPP 层新增"读取原始数据"接口，再由 Dart 内部组件解析渲染。以视频为例，完整流程如下（音频/PDF/电子书同理）：

### 步骤 1：在 file_ops.h 添加通用字节读取声明（已有 `read_file_bytes` 可直接复用）

```c
// 已有通用接口：读取整个文件为字节数组（已存在，无需新增）
unsigned char* file_ops_read_file_bytes(const char* path, int *out_len);
```

> 若需要流式/分块读取大文件，可新增：
> ```c
> // 新增：读取文件指定区间原始字节（视频/音频流式播放用）
> unsigned char* file_ops_read_file_range(const char* path, int64_t offset, int64_t length, int *out_len);
> ```

### 步骤 2：在 file_ops_json.h 添加 JSON/二进制封装

二进制数据不适合 JSON 传输，建议新增返回 base64 的封装（供大文件分块）或直接返回字节：

```c
// 新增：读取视频/音频/PDF/电子书原始数据为 base64
char* file_ops_json_read_binary_as_base64(const char* path);
// 或复用已有的 file_ops_json_read_image_as_base64（图片专用），新增通用版
```

### 步骤 3：在 file_ops_json.cpp 实现（复用 file_ops_read_file_bytes）

```cpp
char* file_ops_json_read_binary_as_base64(const char* path) {
    // 复用已有的 base64 编码逻辑，通用化读取
    int data_len = 0;
    unsigned char* data = file_ops_read_file_bytes(path, &data_len);
    if (!data || data_len == 0) {
        return strdup("{\"error\":\"read failed\",\"base64\":\"\"}");
    }
    // ... base64 编码（与 read_image_as_base64 相同实现）...
}
```

### 步骤 4：在 file_ops_bindings.dart 绑定新函数

```dart
typedef JsonPathNative = Pointer<Utf8> Function(Pointer<Utf8>);
typedef JsonPathDart = Pointer<Utf8> Function(Pointer<Utf8>);

late final JsonPathDart readBinaryAsBase64;

readBinaryAsBase64 = _lib.lookupFunction<JsonPathNative, JsonPathDart>(
  'file_ops_json_read_binary_as_base64');
```

### 步骤 5：在 FileService 中新增通用数据读取方法

```dart
Uint8List? readFileData(String path) {
  final pp = path.toNativeUtf8();
  try {
    final str = _callJson(() => _n.readBinaryAsBase64(pp));
    final j = jsonDecode(str);
    if (j['error'] != null && j['error'] != '') return null;
    return base64Decode(j['base64'] as String);
  } catch (_) {
    return null;
  } finally {
    calloc.free(pp);
  }
}

// 派生方法（语义清晰）
Uint8List? readVideoData(String path) => readFileData(path);
Uint8List? readAudioData(String path) => readFileData(path);
Uint8List? readPdfData(String path) => readFileData(path);
Uint8List? readEbookData(String path) => readFileData(path);
```

### 步骤 6：在 APP 内部 viewer 组件中使用数据渲染

```dart
// VideoPlayerPage 内部渲染示例（不依赖系统播放器）
class VideoPlayerPage extends StatefulWidget {
  final String path;
  ...
}

class _VideoPlayerPageState extends State<VideoPlayerPage> {
  Uint8List? _data;
  @override
  void initState() {
    super.initState();
    _data = FileService().readVideoData(widget.path);
  }
  @override
  Widget build(BuildContext context) {
    if (_data == null) return const Center(child: Text('无法读取视频'));
    // 使用 APP 内部集成的视频渲染组件（如自研解码器/内置播放器控件）
    return AppInternalVideoPlayer(data: _data!);
  }
}
```

> 其余 viewer（音频/PDF/电子书）同理，在 APP 内部组件中解析 `readFileData` 返回的字节数据并渲染，**全程不调用任何系统程序**。

### 完整"点击打开文件"调用链（APP 内部）

```
用户点击文件
  → _handleTap → _openEntry
    → 根据类型路由到 APP 内部 viewer 组件
      → FileService.readFileData(path)   // 新增：CPP 读取原始数据
        → FileOpsNative.readBinaryAsBase64 (FFI)
          → file_ops_json_read_binary_as_base64 (C++ JSON 封装)
            → file_ops_read_file_bytes (C++ 核心，内部读取字节)
      → Dart 内部组件解析数据并渲染（不依赖系统）
```

---

## 十一、构建与运行

- 构建脚本：`build_linux.sh`（检查依赖 → `flutter pub get` → 清理 → `flutter build linux --release` → 验证产物）
- 产物路径：`build/linux/x64/release/bundle/`
  - 可执行文件：`flutter_file_manager`
  - 原生库：`lib/libfile_ops.so`
- 运行：`build/linux/x64/release/bundle/flutter_file_manager`
- **依赖**：需要 `libssl-dev`（加密用）、`zlib1g-dev`、`cmake`、`g++`

---

## 十二、注意事项与改进建议

1. **文件点击未实现**：`_handleTap` 目前不处理普通文件，这是最需要补充的功能点。
2. **FFI 未绑定完整**：C++ 层已有的 `access/chmod/chown/symlink/link/realpath/readlink` 等接口未在 Dart 绑定层暴露。
3. **`determineViewer` 未使用**：该方法已定义，但尚未在 UI 层调用，可直接用于文件点击路由。
4. **废弃系统打开**：现有 `openFile()` 依赖 `xdg-open`，违反"不依赖系统能力"原则，应废弃并改用 APP 内部组件。
5. **加载方式待改**：`DynamicLibrary.open('libfile_ops.so')` 仅支持 Linux，需改为 `DynamicLibrary.process()` 实现全平台。
6. **viewer 待完善**：视频/音频/PDF/电子书目前是壳页面，需实现 APP 内部渲染组件（数据由 CPP 层 `readFileData` 提供）。
7. **新增数据读取接口**：需在 CPP 层新增 `file_ops_json_read_binary_as_base64` 通用二进制读取接口，供所有 viewer 内部渲染使用。
