build/
.dart_tool/
*.iml
.idea/

# ============================================================
# 第三方预编译库已 vendor 到 third_party/<platform>/（随仓库提交，见 REQUIREMENTS.md）
# 编译直接使用，不再下载。下方仅忽略构建期临时产物。
# ============================================================
build/_thirdparty_build/
build/_thirdparty/

# 构建中间产物（third_party/ 下的 .a 属预编译产物，需保留，故不全局忽略 *.a）
*.o
*.obj
*.d
