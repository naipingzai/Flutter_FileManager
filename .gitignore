build/
.dart_tool/
*.iml
.idea/

# ============================================================
# 第三方依赖（由 fetch_thirdparty.cmake / fetch_ffmpeg.cmake 在
# CMake 配置阶段自动下载到 ${CMAKE_BINARY_DIR}/_thirdparty_build/）
#
# 遵循 skill 规范：APP 不编译第三方库源码，只消费
# Flutter_CrossPlatformDependency 仓库 Release 发布的静态库产物。
# 不提交到 git，避免仓库过大、跨平台 ABI 不兼容。
# ============================================================
build/_thirdparty_build/
build/_thirdparty/

# 第三方源码/归档临时文件
*.tar.xz
*.tar.gz

# 构建中间产物
*.a
*.o
*.obj
*.d
