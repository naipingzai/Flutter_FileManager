
#include "Generated.xcconfig"

// 链接原生静态库（libflutter_native.a = core/media + 预编译 FFmpeg，force_load 保持全部符号供 FFI）
OTHER_LDFLAGS=$(inherited) \
  -Wl,-force_load,$(PROJECT_DIR)/../build/native/ios/libflutter_native.a \
  -Wl,-export_dynamic \
  -lz -framework AudioToolbox -framework CoreFoundation
